name: Browser Dark Mobile Fast Audit

on:
  push:
    branches:
      - fix-dark-contrast-round2-20260915
  workflow_dispatch:

permissions:
  actions: read
  contents: read

concurrency:
  group: browser-dark-mobile-fast-audit
  cancel-in-progress: true

jobs:
  mobile-products:
    name: mobile-products-${{ matrix.shard }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      max-parallel: 4
      matrix:
        shard: [0, 1, 2, 3]
    steps:
      - name: Checkout audit harness
        uses: actions/checkout@v7
        with:
          ref: browser-dark-contrast-audit-20260915
      - name: Download shared product catalog
        uses: actions/download-artifact@v4
        with:
          name: browser-dark-product-catalog
          path: browser-audit-discovery
          repository: BigOfertas/BIGOFERTASSS
          run-id: 34922517490
          github-token: ${{ github.token }}
      - name: Patch audit to reuse shared catalog
        run: node scripts/patch-browser-audit-discovery.mjs
      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - name: Install app dependencies
        run: bun install --frozen-lockfile
      - name: Install browser audit dependencies
        run: npm install --no-save playwright axe-core
      - name: Install Chromium
        run: npx playwright install --with-deps chromium
      - name: Audit every mobile product page in real Chromium
        env:
          AUDIT_MODE: products
          AUDIT_VIEWPORT: mobile
          AUDIT_SHARD_INDEX: ${{ matrix.shard }}
          AUDIT_SHARD_TOTAL: 4
          AUDIT_PRODUCT_LINKS_FILE: browser-audit-discovery/product-links.json
        run: node scripts/browser-dark-contrast-audit.mjs
      - name: Upload mobile browser report and screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: browser-dark-mobile-fast-${{ matrix.shard }}
          path: browser-audit/
          if-no-files-found: error
          retention-days: 3
